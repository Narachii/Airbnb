.message#append{style: "clear:both;"}
	.chat-box
		.chat-area
			.chat-face.temp__face
				= link_to user_path(id: message.user.id) do
					= image_tag message.user.image_url,class: "profile__avatar message__avatar"
				.message__name.temp__name
					= message.user.first_name
			.chat-hukidashi.temp__hukidashi
				= message.body
				%br
				%br
					= message.created_at.strftime('%Y/%m/%d %H:%M')
			%span{style: "display:none;"}
				= message.user.id